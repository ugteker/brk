# Brokerino — Single Source of Truth

> **This file is the canonical, living record of requirements and progress.**
> It supersedes `scope-and-decisions.md` and `status.md` (kept for history only).
> Updated automatically after every completed task or new requirement.

Last updated: 2026-07-12 (Richer per-agent dashboard data: Sources/Runs/Reports/Schedule/emails)

---

## 1. Requirements (cumulative, append-only unless explicitly superseded)

### Product Direction
- Brokerino bots are **AI agents**: they crawl configured sources, feed evidence + a
  system prompt into the **Anthropic Claude API (Sonnet model)**, and produce a
  structured report of stock signals.
- Report output = **long/short signal per symbol**, with **confidence**,
  **rationale**, and **source citations/timecodes**. Informational only —
  **no automated trade execution, brokerage write-back, or live market-data platform.**
- If a bot crawls a podcast, the report must reflect everything discussed and
  indicate which symbols are suggested long vs. short based on the conversation.
- User has an Anthropic Claude API key (API-based account) — integration must call
  the real Anthropic API in production, but stay **testable without a live Claude
  dependency** (inject a fake client in tests).

### Architecture / Backend
- Stack: TypeScript, Fastify, Prisma, **SQLite** for local dev (not Postgres).
- Keep the existing scheduler/worker shape — no separate orchestration stack.
- Existing Prisma model names (`Bot`, `BotRun`, etc.) may remain as-is during the
  transition even as product language shifts from "bot" to "agent".
- The web app must keep proxying `/api` requests to the API during local dev.
- The dashboard must load persisted agents/reports (not stay local-only).

### Frontend
- Primary UI library: **Ant Design** (chosen as "most popular" per user request for
  something "fancy"), replacing the shadcn-style component set for the redesigned
  screens.
- 6-step agent setup flow: identity → sources/ingestion rules → system prompt →
  signal policy/publish rules → schedule/recipients → review and run.
- **Reports browsing view**: once a bot/agent produces a report, users must be able
  to navigate through all related reports for that agent. Each report list item
  must show:
  - Badges for every stock symbol mentioned in that report (long/short colored).
  - The date the report was created.
  - A very short summary/title/headline describing the report.
  - A visual indicator of how confident/reliable the report's signals are
    (e.g. a progress/gauge-style confidence indicator), not just a number.
- **Bot pause/resume**: each bot must have a pause/resume icon control (not a
  text button) so the user can toggle a bot between active and disabled
  directly from the dashboard/detail view.
- **Wizard step headers**: the 6-step agent setup wizard's step headers must
  use icons + short titles (not long squeezed text labels).
- **Dark theme correctness**: dark mode must apply consistently to *all*
  elements, including Ant Design inputs/cards/selects — not just
  Tailwind-styled elements.
- **Theme toggle control**: the theme switch must be a single icon button
  (sun/moon), not a dropdown/select.
- **Trading persona prompt library**: the wizard's System prompt step must
  offer multiple full, ready-to-use system prompts ("personas") with
  different trading characters/risk profiles that the user can pick from,
  each of which also sets a sensible default risk level.
- **Full-field agent editing**: the wizard must support editing every setting
  of an existing agent (name, description, sources, preferences, recipients,
  schedule), not just name/sources — this required persisting previously
  ignored fields end-to-end (description, preferences JSON, recipients JSON,
  and a real `AgentSchedule` row) so the scheduler actually has something to
  pick up.
- **Smart, AI-assisted crawling**: agents must not reprocess the same content
  every run. Feed-like sources (RSS/Atom, podcast feeds) use a deterministic
  cursor (seen-item-ID tracking); non-feed sources (blogs/listing pages) get
  a one-time Claude "site inspection" call that produces a persisted crawl
  config (selectors/patterns), applied deterministically on every subsequent
  run, with self-healing re-inspection if the site's markup changes (capped
  at 1 reinspection attempt per source per 24h).
- **Fail-fast source probing**: the wizard must let the user "Test source" a
  URL/feed *before* saving the agent, showing whether it's reachable, what
  kind of source it looks like (feed/listing page/single page/unknown), an
  item count, and a confidence score — without waiting for the first
  scheduled run.
- **Per-source episode/item counter**: each source must have a configurable
  "episodes/items per run" cap (`maxItems`, default 1, range 1-10) instead of
  a hardcoded constant, surfaced directly in the wizard next to the source
  URL.
- **Manual "run agent now"**: users must be able to trigger an immediate
  agent run from the dashboard, not just wait for the next scheduled tick.
- **Agent change confirmation emails**: recipients must get an email whenever
  an agent they're subscribed to is created or updated.
- **Authentication**: username/password signup+login with two-step email
  confirmation, Google social login, forgot/reset password, and an
  admin-only user management view (lock/unlock/delete users) restricted to
  the `ADMIN_EMAIL` configured in env. All `/api/agents`/`/api/admin` routes
  must be gated behind a valid session.
- **Runs history view**: the agent detail view must show a "Runs" tab with
  execution history (status, scheduled/started/finished time, duration,
  retry count), the real failure reason for failed runs (not just an opaque
  error code), a preview of crawled content per source with a full-content
  download button, and a link that jumps to/highlights the corresponding
  report in the Reports tab.
- **Weekly schedule mode**: in addition to interval and daily-time modes, the
  wizard must support a weekly mode where the user picks specific days of
  the week plus a daily time.
- **Multi-recipient emails**: the wizard's recipients field must be a proper
  multi-email tags input (not a single plain-text field the user has to know
  to comma-separate), and must default to the logged-in user's own email for
  new agents.
- **Active toggle must take effect**: the wizard's "Active" toggle must
  actually set the agent's status (`active`/`disabled`) on create/update —
  not be purely cosmetic — while the per-source "Enabled" toggle and the
  "Active" toggle are both intentionally kept (user explicitly reversed
  earlier removal decisions for both).
- **Live-updating Reports/Runs views**: the dashboard's Reports and Runs
  views should reflect new reports / run-status changes without requiring a
  manual page reload.
- **Robust JSON parsing of Claude responses**: the analysis pipeline must
  tolerate Claude wrapping its JSON response in markdown code fences
  (` ```json ... ``` `) instead of crashing with a raw `JSON.parse` error.
- **YouTube as a source type**: alongside `web_urls` and `podcast_feeds`,
  agents must support YouTube sources — a single video, or a channel/playlist
  URL that's auto-resolved to list its videos so the N most recent
  *unseen* episodes (per the existing episode-count setting) are crawled
  automatically, with full video transcripts fetched (not just show notes),
  rather than requiring the user to add every video URL manually.

### Process / Workflow (user-directed, applies across this session and future work)
- **All requirements must live in a single markdown source of truth (this file)** —
  do not scatter requirements across chat history only.
- **This file must be updated automatically as progress is made** (after each task,
  not just at the end).
- Since this repo has no git history, implementation proceeds via direct
  session-based execution (TDD per task) rather than full subagent-driven-development
  tooling, which assumes a git repo.

---

## 2. Progress Ledger

Plan reference: `docs/superpowers/plans/2026-07-10-brokerino-agent-ai-redesign.md`
Spec reference: `docs/superpowers/specs/2026-07-10-brokerino-ai-agent-redesign-design.md`

| Task | Status | Notes |
|---|---|---|
| Task 1: Persist prompt versions, artifacts, run reports | ✅ Done | Prisma models `BotPromptVersion`, `BotRunArtifact`, `BotRunReport`, `BotSignal` added; `PromptRepository`, `ArtifactRepository`, `ReportRepository` implemented with tests (7 tests passing). `prisma generate` + `db push` run successfully against local SQLite dev.db. |
| Task 2: Claude analysis pipeline + source adapters | ✅ Done | `@anthropic-ai/sdk` added to `apps/api`. Implemented `WebUrlAdapter`, `PodcastFeedAdapter` (transcript-first, show-notes fallback with `fidelity: 'low'`), `buildAnalysisRequest`/`renderEvidenceForPrompt`, `parseClaudeResponse` (with validation errors), `ClaudeClient` (DI-able Anthropic messages client). 11 tests passing. |
| Task 3: Wire scheduled runs to agent execution + `/api/agents` routes | ✅ Done | Added `AgentRunner` (crawls sources via adapters, saves artifacts, calls Claude, saves report); added `Bot.getBot()` to repository/interface; rewired `worker.ts`/`scheduler-loop.ts` to run the agent pipeline instead of auto-succeeding; added `/api/agents/:botId/report/latest`, `/api/agents/:botId/reports` (list), `/api/agents/:botId/prompt/latest`, `POST /api/agents/:botId/prompt`; wired all of it in `main.ts`/`server.ts`. Full API suite: **41/41 tests passing**, `tsc` build clean. |
| Task 4: Rebuild web experience with Ant Design | ✅ Done | `antd` + `@ant-design/icons` installed. Added `listReportsForBot` to `ReportRepository` + `/api/agents/:botId/reports` route to support the **reports browsing view** requirement (badges per symbol, date, headline, confidence indicator). Built `apps/web/src/api/agents.ts` client, `AgentSignalReport`, `AgentReportsBrowser`, `AgentPromptEditor` components (all tested; jsdom needed `matchMedia`/`ResizeObserver` polyfills in `src/test-setup.ts`). Rebuilt `BotForm.tsx` as a 6-step Ant Design wizard (Agent identity → Sources & ingestion → System prompt → Signal policy & publish rules → Schedule & recipients → Review & run) that creates the bot then saves its prompt version. Rebuilt `BotsPage.tsx` with Ant Design `Layout`/`Card`/`Tabs`: selecting a bot now shows its **Reports** tab (`AgentReportsBrowser`, fulfilling the reports-browsing requirement) and **System prompt** tab (`AgentPromptEditor`). Updated `BotStatusCard` to Ant Design; wrapped app in `ConfigProvider` + `antd/dist/reset.css` in `main.tsx`. `ThemePicker` intentionally left as a native `<select>` to preserve its `fireEvent.change` test contract. Full web suite: **16/16 passing**; full API suite: **41/41 passing**; `vite build` clean on both. |
| Task 5: End-to-end verification + setup docs | ✅ Done | Added `apps/api/src/modules/analysis/agent-runner.integration.test.ts` — exercises the real `BotRepository`/`PromptRepository`/`ArtifactRepository`/`ReportRepository` classes (backed by an in-memory fake Prisma client, following the repo's existing mocking convention) wired into `AgentRunner` with a stubbed Claude messages client, proving a full crawl → Claude → persisted-report round trip. Added `apps/web/playwright.config.ts` (`testDir: './e2e'`) to stop Playwright from colliding with Vitest unit tests, and `apps/web/e2e/agent-flow.spec.ts` (synthetic-DOM flow: dashboard → create agent → system prompt step → report card with badge/date/headline/confidence), matching the existing `bot-setup.spec.ts` convention. Updated `docs/implementation/setup-and-run.md` with Anthropic API key setup (`ANTHROPIC_API_KEY`), a note that Claude model comes from the stored prompt version (not an env var), web app run instructions, full/targeted test commands, and the Windows Prisma file-lock workaround. Final verification: **API 42/42 tests passing**, `tsc -p tsconfig.build.json` clean; **Web 16/16 unit tests passing**, `vite build` clean, **2/2 Playwright e2e specs passing**. |
| Task 6: UI polish — pause/resume, wizard icons, dark theme fix, persona library | ✅ Done | **Backend**: added `enableBot` (mirrors `disableBot`) to `BotRepositoryLike`, Prisma `BotRepository`, `InMemoryBotRepository`, and a new `POST /api/bots/:botId/enable` route (10/10 `src/modules/bots` tests passing). **Frontend theme fix**: added `apps/web/src/theme/ThemeContext.tsx` (`ThemeProvider`/`useTheme`, persists to `localStorage`, respects `prefers-color-scheme`) and wired `main.tsx`'s antd `ConfigProvider` to switch `algorithm` between `defaultAlgorithm`/`darkAlgorithm` based on theme state — this is what actually fixes antd inputs/cards staying light in dark mode (previously only a Tailwind `.dark` class was toggled, which antd ignores). `ThemePicker.tsx` rebuilt as a single icon-only circular button (`SunOutlined`/`MoonOutlined`) instead of a dropdown. **Wizard step icons**: `BotForm.tsx`'s `STEPS` changed from plain strings to `{title, icon}` pairs (`UserOutlined`, `LinkOutlined`, `MessageOutlined`, `SafetyCertificateOutlined`, `ClockCircleOutlined`, `CheckCircleOutlined`) with short titles (Identity/Sources/Prompt/Policy/Schedule/Review) and `Steps` using `titlePlacement="vertical"` to stop the squeezed-header layout. **Persona library**: new `apps/web/src/data/prompt-personas.ts` with 7 full, ready-to-use Claude system prompts (Conservative Analyst/low, Balanced Analyst/medium [default], Aggressive Momentum Trader/high, Contrarian Value Investor/medium, Quant/Data-Driven Analyst/medium, Short-Seller/Skeptic/high, Macro/Thematic Strategist/medium) — wired into the System prompt step via a new "Persona" `Select` that swaps the prompt text and defaults the Signal Policy step's risk level to the persona's suggested level (still user-overridable). **Pause/resume UI**: `apps/web/src/api/bots.ts` gained `enableBot`/`disableBot` client calls; `BotsPage.tsx` now shows a pause/resume icon button (`PauseCircleOutlined`/`PlayCircleOutlined`) on each dashboard bot card and in the selected-bot detail header, wired to the new API calls with a refetch after toggling. Updated `BotForm.test.tsx` to wrap all `BotsPage` renders in `<ThemeProvider>` and switched the theme-toggle test from `fireEvent.change` on a select to `fireEvent.click` on the icon button. Final verification: **Web 16/16 unit tests passing**, `vite build` clean; **API 10/10 `bots` module tests passing**; both dev servers (API `:3000`, web preview `:4173`) restarted and confirmed responding (`200`) after the change. |
| Task 7: Remove-agent feature | ✅ Done | **Backend**: `BotRepository.deleteBot()` cascade-deletes signals→reports→artifacts→runs→prompt versions→schedules→sources→bot inside a `$transaction` (required because all `Bot`-related FKs use `onDelete: Restrict`); added to `BotRepositoryLike`, `InMemoryBotRepository`, and a new `DELETE /api/bots/:botId` route (204 / 404). **Frontend**: `apps/web/src/api/bots.ts` gained `deleteBot()`; `BotsPage.tsx` shows a `Popconfirm`-guarded `DeleteOutlined` icon button on each dashboard bot card and in the selected-bot detail header, resetting selection and refetching after deletion. Final verification: **API 47/47 passing** (at time of feature), **Web 17/17 passing**, `vite build` clean. |
| Task 8: Authentication (username/password + Google) + configurable SMTP | ✅ Done | **Scope decisions (autonomous, user unavailable to confirm at the time — flagged for review)**: JWT-in-httpOnly-cookie sessions; all existing `/api/bots`+`/api/agents` routes gated behind auth; SMTP implemented as backend config + mailer service only (no email-sending feature wired to a trigger yet); config sourced from `.env`/env vars (`apps/api/.env.example`). **Backend**: new `apps/api/src/config.ts` (lazy-getter accessors — required so `vi.stubEnv()` per-test isolation works — covering `auth.jwtSecret/jwtExpiresIn/cookieName/cookieSecure`, `auth.google.*`, `auth.bootstrapAdmin.*`, `smtp.*`, `appBaseUrl`); Prisma `User` model (email unique, optional `passwordHash`/`googleId`); `modules/auth/{types,password,jwt,repository,in-memory-user-repository,google-oauth,mailer,routes}.ts`; routes: `POST /api/auth/signup`, `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me`, `GET /api/auth/google` (redirect), `GET /api/auth/google/callback` (find-or-create-or-link by googleId/email, sets session cookie). `server.ts` registers `@fastify/cookie` (pinned to `^9` for Fastify v4 compat) and an `onRequest` guard hook blocking all unauthenticated `/api/*` calls except `/api/auth/*`. `main.ts` wires `UserRepository`/`GoogleOAuthHttpClient` and an optional `ADMIN_EMAIL`/`ADMIN_PASSWORD` bootstrap-admin account on startup (additive to self-service signup, not a replacement for it). Google OAuth implemented via raw `fetch` (no OAuth library dependency). `toAuthUser()` never exposes `passwordHash`, only `hasPassword`/`hasGoogleLinked` booleans. New `test-utils/auth.ts` (`createTestAuthDeps()`/`authCookieHeader()`) is now the standard convention for any protected-route test. **Frontend**: `apps/web/src/api/auth.ts` (signup/login/logout/getCurrentUser/`GOOGLE_SIGN_IN_URL`); `apps/web/src/auth/AuthContext.tsx` (`AuthProvider`/`useAuth`, checks `/api/auth/me` on mount); `apps/web/src/pages/AuthPage.tsx` (Ant Design card with a Login/Sign up `Segmented` toggle, email+password form, and a "Sign in/up with Google" button linking to `/api/auth/google`); `App.tsx` now wraps the app in `AuthProvider` and gates `BotsPage` vs `AuthPage` on session state (with a loading spinner while `/me` resolves); `BotsPage.tsx` header now shows the logged-in user's name/email and a logout icon button. `vite.config.ts` given a matching `preview.proxy` (in addition to `server.proxy`) so `/api` calls are proxied to the API in both `vite dev` and the `vite preview` deployment mode used by this repo's dev servers. Final verification: **API 21 files / 62 tests passing**, `tsc -p tsconfig.build.json --noEmit` clean; **Web 5 files / 21 tests passing**, `vite build` clean; live end-to-end curl flow through the web proxy confirmed signup → login → `/me` → protected `/api/bots` (200) → logout (204) → protected route blocked (401). |
| Task 9: Rename "Bot" → "Agent" everywhere | ✅ Done | Full codebase rename, user-requested. **Backend**: Prisma models `Bot→Agent`, `BotSource→AgentSource`, `BotSchedule→AgentSchedule`, `BotRun→AgentRun`, `BotPromptVersion→AgentPromptVersion`, `BotRunArtifact→AgentRunArtifact`, `BotRunReport→AgentRunReport`, `BotSignal→AgentSignal` (dev.db reset via `prisma db push --force-reset`, no production data at stake); module folder `modules/bots``modules/agents` (CRUD: `AgentRepository`, `registerAgentRoutes`, routes now `/api/agents`, `/api/agents/:agentId`, etc.); the **pre-existing** `modules/agents` (prompt/report routes) renamed to `modules/agent-prompts` with `registerAgentPromptRoutes`/`AgentPromptRoutesDeps` to avoid a naming collision with the newly-renamed CRUD module — both still mount under the same `/api/agents/*` URL space without conflict. **Frontend**: `BotForm.tsx→AgentForm.tsx`, `BotsPage.tsx→AgentsPage.tsx`, `BotStatusCard.tsx→AgentStatusCard.tsx`; `api/bots.ts` merged into `api/agents.ts` (single client module for both CRUD and prompt/report calls). All identifiers (`createBot→createAgent`, `listBots→listAgents`, `selectedBot→selectedAgent`, etc.) and UI copy ("Bot dashboard"→"Agent dashboard", "Create Bot"→"Create Agent") renamed via a word-boundary-safe scripted pass (careful to exclude false positives like `both`/`marginBottom`). Playwright e2e spec `bot-setup.spec.ts` renamed to `agent-setup.spec.ts`. Final verification: **API 21 files / 62 tests passing**, `tsc -p tsconfig.build.json --noEmit` clean; **Web 5 files / 21 tests passing**, `vite build` clean. |
| Task 10: Full-field agent wizard editing | ✅ Done | Extended the wizard to support editing every field of an existing agent (description, per-source config, preferences JSON, recipients JSON, and a real persisted `AgentSchedule` row), not just name/sources. Removed a duplicate "Complete" button bug. Wizard now round-trips through `AgentDetail` correctly in both create and edit mode. |
| Task 11: AI-assisted smart crawler | ✅ Done | Implemented a two-tier crawl strategy: feed-like sources (RSS/Atom, podcast feeds) use deterministic cursor/seen-item-ID tracking; non-feed sources (blogs/listing pages) get a one-time Claude "site inspection" call that derives a persisted crawl config (selectors/patterns) reused on subsequent runs, with self-healing re-inspection (capped at 1/24h) if the site's markup changes. Added a wizard "Test source" (probe) feature so users get fail-fast feedback (reachability, detected source kind, item count, confidence) before saving. |
| Task 12: Mobile UX fixes | ✅ Done | Fixed mobile viewport zoom issues, wizard layout on small screens, a `TouchSafeTooltip` double-tap bug (first tap opened tooltip instead of activating the control), and added a sticky wizard footer so Next/Back/Complete stay reachable while scrolling. Also fixed a Netskope corporate TLS CA issue that broke outbound HTTPS calls (Claude API, feed fetches) by default on this machine — now handled automatically on API startup. |
| Task 13: Manual run + agent change emails | ✅ Done | Added a "Run now" icon button (backend trigger route + frontend wiring) to run an agent immediately instead of waiting for its schedule. Added `sendAgentChangeConfirmation` emails (via the existing SMTP mailer service from Task 8) sent to recipients whenever an agent is created or updated. |
| Task 14: Two-step signup, password reset, admin user management | ✅ Done | Extended Task 8's auth: email-confirmation-gated signup (two-step, confirmation link required before login), forgot-password/reset-password flow (emailed token), and an admin-only user management view (list/lock/unlock/delete users) restricted to the `ADMIN_EMAIL` configured in env. Also fixed an admin-bootstrap lockout bug where the bootstrap admin account could get stuck unable to log in because it was never marked as email-verified. |
| Task 15: Runs history feature | ✅ Done | **Backend**: `RunsRepository` + routes exposing per-agent run history (status, scheduled/started/finished timestamps, duration, retry count) and per-run crawled-content previews with a full-content download endpoint. **Frontend**: `AgentRunsBrowser` component (new "Runs" tab on the agent detail view) plus a link from a run's report reference that jumps to and highlights the corresponding entry in the Reports tab. |
| Task 16: Runs error reason, weekly schedule, wizard cleanup pass 1 | ✅ Done | Fixed a bug where `AgentRunner.run()`'s bare `catch {}` silently discarded the real failure reason (only an opaque `errorCode` was ever saved) even though partial evidence/artifacts had already been persisted — now the actual error message is captured and surfaced through the Runs view. Removed the dead, purely-decorative per-source `frequencyMinutes` field (confirmed via grep it was never read by the scheduler, only shown in confirmation-email text). Replaced the single-line recipients `Input` with a proper AntD `Select mode="tags"` multi-email input. Added a new `weekly` schedule mode (day-of-week `Checkbox.Group` + daily time), implemented end-to-end (Prisma schema, `compute-next-run.ts`, validation, repository, wizard UI). |
| Task 17: Policy step removal + episode counter + wizard toggle cleanup | ✅ Done | Investigated and confirmed all 4 "Policy" step settings (`confidence_threshold`, `risk_level`, `notification_cadence`, `report_detail_level`) were completely inert — stored in `preferences` JSON but never read back anywhere in the backend (no confidence filtering, no cadence-based emails, since no per-run notification pathway exists) — so removed the Policy step entirely per user request. Added a real per-source `maxItems` field (default 1, range 1-10, "episodes/videos per run") end-to-end (schema, validation, repository, crawler, wizard `InputNumber`), replacing the old hardcoded `MAX_NEW_ITEMS_PER_RUN = 3` constant. Fixed a Sources-step grid layout bug (`md:col-span-4` inside a `md:grid-cols-3` parent). The per-source "Enabled" toggle and the wizard's "Active" toggle were both removed and then explicitly restored per user follow-up (`do not remove the "Enabled" toggle`, `keep the Active Toggle`) — both are intentionally present in the final wizard. |
| Task 18: Wire Active toggle to real status + default recipients | ✅ Done | The wizard's "Active" toggle was previously cosmetic (saved into `preferences.active` but never read back). Added a first-class `active?: boolean` field to `CreateAgentInput`; `createAgent`/`updateAgent` now set `status: 'active'`/`'disabled'` accordingly (`disabled` reuses the existing dashboard pause-state, no new status value introduced) — confirmed: toggling Active on/off in the wizard now actually activates/pauses the agent, matching the dashboard's pause button. Also defaulted the wizard's recipients field to the logged-in user's own email (via `useAuth()`) for **new** agents only (existing agents keep their saved recipients when editing). |
| Task 19: AgentForm test-flakiness fix + YouTube source type | ✅ Done | **Test fix**: root-caused an intermittent `AgentForm.test.tsx` failure to AntD's imperative `message.success()` toast and an inline `<p>` both rendering identical "Agent saved successfully" text into the DOM simultaneously (toast persists outside RTL's `cleanup()`), causing nondeterministic "multiple elements found" errors — fixed by giving the inline paragraph a `data-testid="agent-save-state"` and querying by that; also fixed an unrelated test timeout and a test asserting on transient UI text that got unmounted by `onComplete?.()`. **YouTube source type**: added `youtube_videos` as a third source type (alongside `web_urls`/`podcast_feeds`) with a new `YouTubeAdapter` — single-video URLs, or channel/playlist URLs auto-resolved via YouTube's public Atom feed (`/feeds/videos.xml?playlist_id=`/`?channel_id=`, no API key needed, `@handle` URLs resolved via an HTML channelId lookup) to automatically pick the N most recent *unseen* episodes (reusing the existing `maxItems`/cursor-tracking conventions from the smart crawler). Transcripts are fetched from the watch page's embedded caption-track metadata (preferring manual English captions over auto-generated) and flattened from timed-text XML to plain text — this fetches the **full spoken transcript**, not just show notes/description text. Added a wizard "YouTube (video/playlist/channel)" source-type option with dynamic placeholder text and a "Videos per run" label. 15 new adapter tests; full backend (156/156) and frontend (44/44) suites green, both builds clean. |
| Task 20: Sneak-preview list on "Test source" probe | ✅ Done | `SourceProbeResult` gained a `previewItems` array (title, link, pubDate). Initially capped to the source's `maxItemsPerRun`, then changed (per user follow-up) to **always show the last 5 items found** (`PREVIEW_ITEM_COUNT = 5`), independent of the "episodes/videos per run" setting, so the count-per-run field only controls actual crawl volume, not preview visibility. Implemented for the generic feed probe (`smart-crawler.ts`'s `probeSource`, via a shared `toPreviewItems` helper) and the YouTube probe (`probeYouTubeSource`): single-video sources preview the resolved video's title (scraped from its `og:title` meta tag) + link; playlist/channel sources preview up to the last 5 feed entries. Also fixed a bug where the wizard's "Test source" button never actually sent the configured `maxItems` to the backend probe endpoint at all (`probeSource` client call + `/api/agents/sources/probe` route both silently dropped it) — the maxItemsPerRun figure shown in the probe result was always the default (1) regardless of the wizard's episode-count input; now passed through end-to-end. Added a note (surfaced as an informational warning) when a YouTube feed hits YouTube's hard 15-item cap on its public playlist/channel Atom feed (confirmed via live curl against a 500+ video playlist — YouTube truncates this feed to the 15 most recent uploads regardless of channel size; not an app bug, and not a functional problem since `maxItems` is itself capped at 10). `AgentForm`'s `SourceProbeSummary` renders the preview as a clickable list. Also began investigating a separate, deeper issue reported by the user: actual agent runs against real YouTube videos returning "no new content" — root-caused so far to YouTube's legacy `/api/timedtext` caption-track URLs (extracted from the watch page HTML) returning HTTP 200 with an **empty body** for auto-generated ("asr") caption tracks on real videos, even with proper User-Agent/Referer/session-cookie headers — appears to be a platform-side change requiring the newer InnerTube `get_transcript` API instead of the old signed timedtext URLs for auto-captions; **not yet fixed, tracked as outstanding follow-up work**. Tests: 6 backend (smart-crawler + youtube-adapter + agents/routes), full backend (160/160) and frontend (44/44) suites green, both builds clean; rebuilt and restarted the local web preview server (stale build was still being served) to pick up these fixes. |
| Task 21: Fixed YouTube transcript/caption crawling (PoToken block) + wizard defaults | ✅ Done | Root-caused Task 20's outstanding YouTube transcript bug via live testing (real playlist `PL6P5rY8mrhqrhVgc_pkSOlRLpuGW3CpJ3`, cross-checked against the actively-maintained `youtube-transcript-api` Python library's source): caption-track `baseUrl`s scraped straight off the watch page's `ytInitialPlayerResponse` are signed with an `exp=xpe` flag that YouTube now silently rejects for unauthenticated/non-browser requests (HTTP 200, empty body — no error to catch), effectively requiring a browser-generated "PO Token" that this app has no way to produce. **Fix**: `youtube-adapter.ts` now fetches the caption track list via YouTube's internal "innertube" `POST /youtubei/v1/player` API impersonating the Android client (`clientName: "ANDROID"`, extracting the required `INNERTUBE_API_KEY` from the watch page HTML) — the Android client's caption URLs carry no PO-Token restriction and download directly. Added a `HttpPostJson` dependency (`defaultHttpPostJson` in `main.ts`) alongside the existing `HttpGet`, threaded through `YouTubeAdapterDeps`/`probeYouTubeSource`. Falls back to the old watch-page-scrape method if the innertube call fails or the API key can't be found. Also fixed `stripTimedTextXml`: the Android client returns a different timed-text XML shape (`<timedtext><body><p t=".." d=".."><s>word</s>...</p></body></timedtext>`) than the legacy `<transcript><text>...</text></transcript>` format the old parser only handled — now generically extracts the `<body>` element (falling back to the whole document for the legacy shape) and strips all tags, handling both. Verified end-to-end against the real playlist (transcript successfully fetched, 123K+ characters of real German spoken content, not just show notes). **Wizard defaults** (per user request): the wizard's initial/default source for a **new** agent is now `youtube_videos` pre-filled with `https://www.youtube.com/playlist?list=PL6P5rY8mrhqrhVgc_pkSOlRLpuGW3CpJ3`; the "Add source" button's new-row default type is also `youtube_videos` (previously both defaulted to `web_urls`/`https://example.com`). Updated `youtube-adapter.test.ts`'s mocks accordingly (innertube JSON response fixture, new-format timed-text fixture, plus fallback-path tests). Full backend (162/162) and frontend (44/44) suites green. |
| Task 22: Fixed Claude JSON response parse failure on markdown-fenced replies | ✅ Done | After Task 21's YouTube fix, a real run against the playlist crawled a full transcript successfully but then **failed** with `agent_run_failed`: `Unexpected token '`', "```json { "... is not valid JSON`. Root cause: `claude-client.ts`'s `ClaudeClient.analyze()` called `JSON.parse(textBlock.text)` directly on Claude's raw response text; despite `RESPONSE_FORMAT_INSTRUCTIONS` asking for "ONLY a JSON object, no prose outside the JSON", Claude sometimes wraps its answer in a markdown ` ```json ... ``` ` code fence anyway — evidently more likely to happen with larger/richer evidence (a full video transcript) than the small evidence blocks exercised by the existing unit tests, so this hadn't previously surfaced. **Fix**: added `extractJsonFromResponseText()` — strips an optional ` ```json `/` ``` ` fenced code block (with or without the `json` language tag) before parsing, with a defensive fallback that extracts the first balanced `{...}` block if Claude adds stray prose outside the JSON entirely. `parseClaudeResponse`'s structural validation is unchanged; only the raw-text-to-JSON-string extraction step was hardened. No changes needed to `agent-runner.ts` — its existing error-message passthrough (from Task 16) is what correctly surfaced this failure's real cause in the first place. Added 3 new tests to `claude-client.test.ts` (fenced-with-`json`-tag, fenced-without-tag, stray-prose-around-JSON), all passing alongside the existing 2; full backend suite green. |
| Task 22: Deployment infra (Hetzner + Cloudflare Quick Tunnel) | ✅ Done | Added `apps/api/Dockerfile` (multi-stage: `prisma generate` + `tsc` build → slim `node:20-alpine` runtime; SQLite `dev.db` persisted via a `/app/prisma` volume) and `apps/web/Dockerfile` (Vite build → self-contained `nginx:alpine` serving the SPA, `apps/web/nginx.spa.conf`). Added root `docker-compose.yml` wiring `api`, `web`, and `cloudflared` (Quick Tunnel mode — no domain owned yet, so the public URL is a random, ephemeral `*.trycloudflare.com` hostname reassigned on every restart/deploy). Secrets pipeline: added `apps/api/.env.example` (template, no real values — the real `apps/api/.env` was already git-ignored/untracked, confirmed via `git check-ignore`); the `api` compose service loads secrets only via `env_file: .env` at runtime (never baked into the image). Added `.github/workflows/deploy.yml` (test `apps/api` + `apps/web`, then SSH into the Hetzner server, materialize `/opt/brokerino/.env` from a `HETZNER_APP_ENV` repo secret via an SSH heredoc, and run `deploy/deploy.sh`) and `deploy/deploy.sh` (`git pull` + `docker compose build` + `up -d`, prints the current tunnel URL from `cloudflared` logs). Full setup, required GitHub secrets (`HETZNER_HOST`/`HETZNER_USER`/`HETZNER_SSH_KEY`/`HETZNER_APP_ENV`), one-time manual server bootstrap steps, and the upgrade path to a named tunnel/Postgres documented in `deploy/README.md`. Verified `docker compose config` parses cleanly. **Not done in this pass** (needs real server access / a registered domain, flagged in `deploy/README.md`): the actual manual first deploy onto the Hetzner box, and rotating the real credentials currently sitting in developers' local `apps/api/.env`. |
| Task 23: Simplified deployment topology (3 containers, not 4) | ✅ Done | User asked "do we really need two docker containers?" re: `web`'s own nginx + a separate top-level reverse-proxy `nginx` service — correctly redundant. Merged them: `apps/web/nginx.spa.conf` now both serves the built SPA (`try_files ... /index.html` fallback) **and** reverse-proxies `/api/*` straight to the `api` container from the same nginx process; removed the standalone `deploy/nginx.conf` and the `nginx` service from `docker-compose.yml`; `cloudflared` now points its Quick Tunnel at `http://web:80` directly. Runtime topology is now `api` + `web` + `cloudflared` (3 containers total, down from 4), with identical routing behavior. Updated `deploy/README.md` diagram/instructions accordingly. Verified `docker compose config` still parses cleanly after the change. |
| Task 24: Collapsed to a single all-in-one container | ✅ Done | User follow-up: "how about ONE container for everything?" Replaced the 3-container setup (`api`/`web`/`cloudflared`) with a **single container** running all three as sibling processes. New root-level `Dockerfile` (multi-stage: `api-build` runs `prisma generate`+`tsc`; `web-build` runs `vite build`; final `node:20-alpine` runtime stage installs `nginx`+`bash` via `apk`, copies both build outputs, and lifts the `cloudflared` binary straight out of `cloudflare/cloudflared:latest` via a `COPY --from=`). New `deploy/entrypoint.sh` starts the Node API, nginx, and `cloudflared` (`tunnel --url http://127.0.0.1:80`) as sibling background processes and shuts all three down if any one exits (`wait -n`), so `restart: unless-stopped` cleanly restarts everything together rather than leaving the container half-working. `deploy/nginx.conf` now proxies `/api/*` to `127.0.0.1:3000` (same container) instead of a separate `api` container. Removed the now-redundant `apps/api/Dockerfile`, `apps/api/.dockerignore`, `apps/web/Dockerfile`, `apps/web/.dockerignore`, `apps/web/nginx.spa.conf`; added a root `.dockerignore` covering both app trees. `docker-compose.yml` collapsed to one `brokerino` service (build context `.`), volume moved to `/app/api/prisma`. Updated `deploy/deploy.sh` and `.github/workflows/deploy.yml` references from `cloudflared`/`web` container names to `brokerino`. **Explicit, documented trade-off** (in `deploy/README.md` and code comments): simplest possible build/ship/restart, at the cost of the usual one-process-per-container convention — no independent scaling/restart of the three processes, and one crash takes all three down; each process keeps its own build stage so splitting back out later is a compose-file change only. Verified `docker compose config` parses cleanly. **Not verified**: an actual `docker build`/`docker compose build` — no Docker daemon was available in this working environment (only the CLI, not a running engine), so the Alpine `nginx` package's config path (`/etc/nginx/http.d/default.conf`) and the `cloudflared` binary `COPY --from=` were written from documentation/GitHub source, not a live build; flagged prominently in `deploy/README.md` to build-test before the first real deploy. |
| Task 25: Re-send email notification icon in Reports view | ✅ Done | Added a manual "re-send report notification" action to the Reports tab — previously reports were never emailed at all (only agent create/update confirmations existed). Backend: `ReportRepository.getReportById()` added; `notifications.ts` gained `buildReportNotificationEmail()`/`sendReportNotification()` (subject/body with summary, signals, needsHumanReview, source warnings), following the same best-effort per-recipient try/catch pattern as the existing `sendAgentChangeConfirmation` (never throws into the request handler). New endpoint `POST /api/agents/:agentId/reports/:reportId/resend-notification` in `agent-prompts/routes.ts` (extended `AgentPromptRoutesDeps` with `agentRepository`/`mailer`): 404 if the agent or report is missing or the report belongs to a different agent, 400 (`no_recipients`) if the agent has no configured recipients, otherwise sends and returns `{ status: 'sent', recipientCount }`. Wired `agentRepository`+`mailer` into `main.ts`'s `agents` deps. Frontend: `resendReportNotification()` added to `api/agents.ts`; `AgentReportsBrowser.tsx` gained an `agentId` prop and a `MailOutlined` icon button (with tooltip, `stopPropagation`, per-report loading state, antd `message` success/error) on each report card; `AgentsPage.tsx` passes `agentId` through. Tests: new `reports/repository.test.ts` cases for `getReportById`, new `agents/notifications.test.ts` (5 tests) for the email builder/sender, 5 new `agent-prompts/routes.test.ts` cases for the endpoint (success + all 404/400 error paths), 3 new `AgentReportsBrowser.test.tsx` cases for the button. Full backend (177/177) and frontend (49/49) suites green. |
| Task 26: Live pending/running runs (any trigger) + duration timer + instant reports | ✅ Done | User asked for the "Runs" tab to show queued/running runs regardless of trigger (schedule vs. manual), a live duration timer for running runs, and reports to appear instantly without a page refresh. Investigation found the backend already fully supported this — `RunsRepository.listRunDetailsForAgent` has no status filter (queued/running rows from either the scheduler loop or `ManualRunTrigger` share the same `AgentRun` DB rows/statuses) and `RunDetailDto` already exposes `startedAt`; the entire gap was frontend-only: `AgentsPage.tsx` fetched runs/reports exactly once per agent selection with no polling, so anything that changed server-side while the page was open (a scheduler-triggered run starting, a report landing) was invisible until the user reselected the agent. **Fix**: `AgentsPage.tsx` now extracts a `refreshAgentDetail()` helper and polls it every 4s (`RUNS_POLL_INTERVAL_MS`) while an agent is selected, cleaned up on unmount/agent change — this alone surfaces pending/running runs from any trigger and newly-completed reports live. `AgentRunsBrowser.tsx` gained a 1s ticking clock (`useEffect`/`setInterval`, only active while at least one `running` run is present, to avoid an always-on timer once everything settles) that renders a live elapsed-duration indicator (small circular `Progress`, `status="active"`, plus "Running for Xs" text computed from `startedAt`) in place of the previous static `Duration —` for in-progress runs; queued/completed runs are unaffected. No backend changes were needed. Tests: 2 new `AgentRunsBrowser.test.tsx` cases using fake timers (`vi.useFakeTimers`/`vi.advanceTimersByTime`) confirming the elapsed time increases live and that queued/completed runs don't show a timer; no `AgentsPage.test.tsx` existed previously, so the polling behavior itself was verified by full-suite regression + manual reasoning rather than adding a first page-level test file for this alone. Full backend (177/177) and frontend (49/49) suites green. |
| Task 27: Clickable trading symbols → per-symbol performance view | ✅ Done | Made each signal `Tag` (e.g. `AAPL · Long`) on a report card clickable, routing to a new in-app performance view for that symbol combining a real market price chart with the agent's own historical signal calls (user confirmed wanting "both", scoped to the currently selected agent). **Backend**: `ReportRepository.listSignalHistoryForSymbol(agentId, symbol)` (`agentRunReport.findMany` filtered by `signals: { some: { symbol } }`, ordered oldest-first) + new `GET /api/agents/:agentId/signals/:symbol` route in `agent-prompts/routes.ts`. **Frontend**: new `TradingViewSymbolChart.tsx` — script-injects TradingView's free public "Advanced Chart" widget (`embed-widget-advanced-chart.js`, no API key/backend needed) for a given symbol, re-initializing on symbol change and cleaning up on unmount; new `SymbolPerformancePage.tsx` (`onBack` prop, same full-page-swap convention already used by `AdminUsersPage`) renders the chart plus a chronological list of this agent's own signal history (fetched via new `listSymbolSignalHistory()` in `api/agents.ts`); `AgentReportsBrowser.tsx` gained an `onSelectSymbol` prop making each signal tag clickable (`stopPropagation` so it doesn't also trigger the card's `onSelectReport`); `AgentsPage.tsx` added `viewingSymbol` state swapping in `SymbolPerformancePage` alongside the existing `showAdminUsers` swap, no new router dependency added. **Known, documented constraint**: the free TradingView widget cannot programmatically overlay custom data series (that needs TradingView's paid Charting Library), so "both" is delivered as the price chart and the signal-history list stacked on the same page rather than a single merged chart — called out to the user during planning. Tests: 4 new `reports/repository.test.ts` cases, 2 new `agent-prompts/routes.test.ts` cases, 2 new `AgentReportsBrowser.test.tsx` cases (symbol click routes correctly and doesn't trigger `onSelectReport`), new `SymbolPerformancePage.test.tsx` (4 tests, TradingView widget mocked). Full backend (182/182) and frontend (55/55) suites green. |
| Task 28: Richer per-agent data on the dashboard list | ✅ Done | User asked for more data per agent card in the dashboard/list view: Sources, Runs, Report, Schedule, emails. Only `Sources: N` was shown before. **Backend**: added `AgentListItem` type (`Agent` + `runCount`/`reportCount`/`latestReportAt`); `AgentRepository.listAgents()` now includes Prisma `_count: { select: { runs: true, runReports: true } }` plus the most recent `runReports` row's `createdAt` in the same query (no extra round-trip), mapped onto each returned agent; `AgentRepositoryLike.listAgents` return type updated accordingly (only the dashboard's `GET /api/agents` route depends on it — no other callers). `schedule`/`recipients` were already present on the full `Agent` row returned by this endpoint, just not surfaced in the frontend type/UI. **Frontend**: `AgentSummary` (`api/agents.ts`) extended with `recipients`, `schedule`, `runCount`, `reportCount`, `latestReportAt`; `AgentsPage.tsx`'s dashboard card now renders `Sources: N · Runs: N · Reports: N (latest <date>)` plus a `Schedule: ... · Emails: ...` line (new `formatAgentSchedule()` helper formats interval/daily/weekly modes), with defensive fallbacks (`?? 0`/`&&`) for any caller/mock that hasn't supplied the new fields. Tests: 2 new `agents/repository.test.ts` cases (default zero/null counts, and populated counts + latest report timestamp). Full backend (183/183) and frontend (55/55) suites green; both `tsc` (api) and `vite build` (web) verified clean. |



## 4. All Tasks Complete

All 5 planned tasks for the Brokerino AI-agent redesign are done and verified, plus three
follow-up batches (Tasks 6-8):
1. Persisted prompt versions, artifacts, and run reports (Prisma models + repositories).
2. Claude analysis pipeline + source adapters (web URL, podcast feed).
3. Scheduled runs wired to real agent execution + `/api/agents/*` routes.
4. Ant Design web rebuild: 6-step agent wizard, reports browser (symbol badges/date/headline/confidence),
   system prompt editor, all wired into `BotsPage`.
5. End-to-end integration test, Playwright e2e flow, and setup docs for Anthropic API key usage.
6. UI polish: bot pause/resume icon controls, wizard step icons + short titles, a working dark
   theme (antd `ConfigProvider` algorithm switching), an icon-only sun/moon theme toggle, and a
   7-persona trading system-prompt library selectable in the wizard.
7. Remove-agent feature: cascade-delete backend route + confirm-guarded delete icon in the UI.
8. Authentication: username/password + Google social login, signup for both methods, JWT
   session cookies, all API routes gated behind auth, and backend-configurable SMTP settings
   (mailer service ready for future email-sending features).

Any further work (e.g., a real live-server e2e run, dead shadcn primitive cleanup, chunk-size
optimization for the `vite build` warning, or actually wiring the SMTP mailer to a triggered
email such as password-reset or signup verification) is optional follow-up, not required by the
original spec.

## 3. Environment Notes
- Repo `G:\brk` is **not git-initialized** — no commits, no branches. Implementation
  proceeds directly in this working tree.
- Local dev servers (API `tsx src/main.ts`, web `vite preview`) must be stopped
  before running `prisma generate`/`db push` on Windows (file lock on the Prisma
  query engine DLL) — restart them after schema changes when needed.
- **The web app is served via `npm run start` (`vite preview`), not `vite dev`** — it serves a
  static `dist/` build, so it does **not** hot-reload on source changes. After editing any
  `apps/web/src` file, you must `npm run build` and restart the preview process (kill the old PID,
  re-run `npm run start`) for changes to actually show up in the browser. The API (`npm run dev`,
  `tsx watch`) does auto-restart on save.

## 5. Known Issues / Outstanding Follow-ups
- **YouTube auto-generated ("asr") captions return an empty transcript in production, and this
  appears to be an IP/anti-bot block rather than a fixable code bug.** The
  `fetchYouTubeTranscript()` flow correctly locates the video's caption track and its signed
  `/api/timedtext` URL from the watch page HTML, but fetching that URL returns HTTP 200 with a
  **0-byte body** for auto-generated caption tracks on real videos — reproduced live via curl/node
  against multiple real YouTube videos, with and without a browser User-Agent, Referer/Origin
  headers, and session cookies from the watch-page request; none of these made a difference.
  **Follow-up investigation (this session): tried the modern InnerTube `youtubei/v1/get_transcript`
  API as a replacement** — both a hand-built protobuf `params` field and the *exact* `params` value
  YouTube itself embeds in the watch page's `getTranscriptEndpoint` JSON (extracted live from a
  real video, using the real `INNERTUBE_API_KEY`/`INNERTUBE_CONTEXT_CLIENT_VERSION` from the same
  page) were POSTed to `youtubei/v1/get_transcript`. **Both attempts failed identically** with
  `400 FAILED_PRECONDITION` ("Precondition check failed"), even though the params/key were sourced
  directly from a fresh, valid watch-page load. Since the *literal* server-supplied params still
  gets rejected, this isn't a params-encoding bug on our side — it strongly indicates YouTube is
  rejecting the request based on request **context/origin** (e.g. missing a browser-session-bound
  visitor/session token, TLS/JA3 fingerprinting, or — most likely, matching widely-reported behavior
  of libraries like `youtube-transcript-api` — an IP-reputation block against cloud/datacenter IP
  ranges, which this server runs on). **Effect**: agent runs against YouTube sources with only
  auto-generated captions will report "no new content" every run, because every candidate video's
  transcript fetch fails, gets logged as a warning, and the video is still marked "seen" (so it's
  not retried forever) — this can look like the crawler is silently exhausting a channel's recent
  videos without ever producing evidence. **Not fixable via request tweaks alone from this
  environment.** Realistic paths forward, none implemented yet:
  1. Route transcript fetches through a residential/rotating proxy (adds cost/complexity).
  2. Use a paid third-party YouTube-transcript API service.
  3. Fall back to crawling the show-notes/description page content only for YouTube sources (already
     the behavior when transcript fetch fails), and set user expectations that full YouTube
     transcripts are not currently retrievable from this server's IP.
  4. Re-test from a non-datacenter IP (e.g. a residential network) to confirm the IP-block theory
     before investing further.
